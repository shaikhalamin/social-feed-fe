/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type {
  GetUserQueryResponse,
  GetUserPathParams,
  GetUser400,
  GetUser401,
  GetUser404,
  GetUser500,
} from '../types/GetUser.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getGetUserUrl({ id }: { id: GetUserPathParams['id'] }) {
  const res = { method: 'GET', url: `/users/${id}` as const }
  return res
}

/**
 * {@link /users/:id}
 */
export async function getUser(
  { id }: { id: GetUserPathParams['id'] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetUserQueryResponse,
    ResponseErrorConfig<GetUser400 | GetUser401 | GetUser404 | GetUser500>,
    unknown
  >({
    method: 'GET',
    url: getGetUserUrl({ id }).url.toString(),
    ...requestConfig,
  })
  return res.data
}
