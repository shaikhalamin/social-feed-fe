/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type {
  ListCommentsQueryResponse,
  ListCommentsPathParams,
  ListCommentsQueryParams,
  ListComments400,
  ListComments401,
  ListComments404,
  ListComments500,
} from '../types/ListComments.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getListCommentsUrl({
  post_id,
}: {
  post_id: ListCommentsPathParams['post_id']
}) {
  const res = { method: 'GET', url: `/posts/${post_id}/comments` as const }
  return res
}

/**
 * {@link /posts/:post_id/comments}
 */
export async function listComments(
  {
    post_id,
    params,
  }: {
    post_id: ListCommentsPathParams['post_id']
    params?: ListCommentsQueryParams
  },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    ListCommentsQueryResponse,
    ResponseErrorConfig<
      ListComments400 | ListComments401 | ListComments404 | ListComments500
    >,
    unknown
  >({
    method: 'GET',
    url: getListCommentsUrl({ post_id }).url.toString(),
    params,
    ...requestConfig,
  })
  return res.data
}
