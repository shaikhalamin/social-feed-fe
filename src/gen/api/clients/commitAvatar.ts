/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type {
  CommitAvatarMutationRequest,
  CommitAvatarMutationResponse,
  CommitAvatarPathParams,
  CommitAvatar400,
  CommitAvatar401,
  CommitAvatar403,
  CommitAvatar404,
  CommitAvatar409,
  CommitAvatar500,
} from '../types/CommitAvatar.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getCommitAvatarUrl({ id }: { id: CommitAvatarPathParams['id'] }) {
  const res = { method: 'POST', url: `/users/${id}/avatar/commit` as const }
  return res
}

/**
 * {@link /users/:id/avatar/commit}
 */
export async function commitAvatar(
  {
    id,
    data,
  }: { id: CommitAvatarPathParams['id']; data: CommitAvatarMutationRequest },
  config: Partial<RequestConfig<CommitAvatarMutationRequest>> & {
    client?: Client
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const requestData = data

  const res = await request<
    CommitAvatarMutationResponse,
    ResponseErrorConfig<
      | CommitAvatar400
      | CommitAvatar401
      | CommitAvatar403
      | CommitAvatar404
      | CommitAvatar409
      | CommitAvatar500
    >,
    CommitAvatarMutationRequest
  >({
    method: 'POST',
    url: getCommitAvatarUrl({ id }).url.toString(),
    data: requestData,
    ...requestConfig,
  })
  return res.data
}
