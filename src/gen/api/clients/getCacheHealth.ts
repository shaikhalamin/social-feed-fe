/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type {
  GetCacheHealthQueryResponse,
  GetCacheHealth500,
} from '../types/GetCacheHealth.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getGetCacheHealthUrl() {
  const res = { method: 'GET', url: `/health/cache` as const }
  return res
}

/**
 * {@link /health/cache}
 */
export async function getCacheHealth(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetCacheHealthQueryResponse,
    ResponseErrorConfig<GetCacheHealth500>,
    unknown
  >({
    method: 'GET',
    url: getGetCacheHealthUrl().url.toString(),
    ...requestConfig,
  })
  return res.data
}
