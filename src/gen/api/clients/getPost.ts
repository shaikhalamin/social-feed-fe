/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type {
  GetPostQueryResponse,
  GetPostPathParams,
  GetPost400,
  GetPost401,
  GetPost404,
  GetPost500,
} from '../types/GetPost.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getGetPostUrl({ id }: { id: GetPostPathParams['id'] }) {
  const res = { method: 'GET', url: `/posts/${id}` as const }
  return res
}

/**
 * {@link /posts/:id}
 */
export async function getPost(
  { id }: { id: GetPostPathParams['id'] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetPostQueryResponse,
    ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>,
    unknown
  >({
    method: 'GET',
    url: getGetPostUrl({ id }).url.toString(),
    ...requestConfig,
  })
  return res.data
}
