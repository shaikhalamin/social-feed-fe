/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/kubb-clients/api-client'
import type { LogoutMutationResponse, Logout500 } from '../types/Logout.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'

function getLogoutUrl() {
  const res = { method: 'POST', url: `/auth/logout` as const }
  return res
}

/**
 * {@link /auth/logout}
 */
export async function logout(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    LogoutMutationResponse,
    ResponseErrorConfig<Logout500>,
    unknown
  >({ method: 'POST', url: getLogoutUrl().url.toString(), ...requestConfig })
  return res.data
}
