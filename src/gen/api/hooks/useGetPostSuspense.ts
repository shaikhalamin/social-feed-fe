/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetPostQueryResponse,
  GetPostPathParams,
  GetPost400,
  GetPost401,
  GetPost404,
  GetPost500,
} from '../types/GetPost.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { getPost } from '../clients/getPost.ts'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getPostSuspenseQueryKey = ({
  id,
}: {
  id: GetPostPathParams['id'] | undefined
}) => [{ url: '/posts/:id', params: { id: id } }] as const

export type GetPostSuspenseQueryKey = ReturnType<typeof getPostSuspenseQueryKey>

export function getPostSuspenseQueryOptions(
  { id }: { id: GetPostPathParams['id'] | undefined },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPostSuspenseQueryKey({ id })
  return queryOptions<
    GetPostQueryResponse,
    ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>,
    GetPostQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getPost(
        { id: id! },
        { ...config, signal: config.signal ?? signal },
      )
    },
  })
}

/**
 * {@link /posts/:id}
 */
export function useGetPostSuspense<
  TData = GetPostQueryResponse,
  TQueryKey extends QueryKey = GetPostSuspenseQueryKey,
>(
  { id }: { id: GetPostPathParams['id'] | undefined },
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetPostQueryResponse,
        ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getPostSuspenseQueryKey({ id })

  const query = useSuspenseQuery(
    {
      ...getPostSuspenseQueryOptions({ id }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>
  > & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
