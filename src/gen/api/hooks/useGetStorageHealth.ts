/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetStorageHealthQueryResponse,
  GetStorageHealth500,
} from '../types/GetStorageHealth.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { getStorageHealth } from '../clients/getStorageHealth.ts'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getStorageHealthQueryKey = () =>
  [{ url: '/health/storage' }] as const

export type GetStorageHealthQueryKey = ReturnType<
  typeof getStorageHealthQueryKey
>

export function getStorageHealthQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getStorageHealthQueryKey()
  return queryOptions<
    GetStorageHealthQueryResponse,
    ResponseErrorConfig<GetStorageHealth500>,
    GetStorageHealthQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getStorageHealth({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * {@link /health/storage}
 */
export function useGetStorageHealth<
  TData = GetStorageHealthQueryResponse,
  TQueryData = GetStorageHealthQueryResponse,
  TQueryKey extends QueryKey = GetStorageHealthQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetStorageHealthQueryResponse,
        ResponseErrorConfig<GetStorageHealth500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getStorageHealthQueryKey()

  const query = useQuery(
    {
      ...getStorageHealthQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetStorageHealth500>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
