/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetCacheHealthQueryResponse,
  GetCacheHealth500,
} from '../types/GetCacheHealth.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { getCacheHealth } from '../clients/getCacheHealth.ts'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getCacheHealthQueryKey = () => [{ url: '/health/cache' }] as const

export type GetCacheHealthQueryKey = ReturnType<typeof getCacheHealthQueryKey>

export function getCacheHealthQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getCacheHealthQueryKey()
  return queryOptions<
    GetCacheHealthQueryResponse,
    ResponseErrorConfig<GetCacheHealth500>,
    GetCacheHealthQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getCacheHealth({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * {@link /health/cache}
 */
export function useGetCacheHealth<
  TData = GetCacheHealthQueryResponse,
  TQueryData = GetCacheHealthQueryResponse,
  TQueryKey extends QueryKey = GetCacheHealthQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetCacheHealthQueryResponse,
        ResponseErrorConfig<GetCacheHealth500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getCacheHealthQueryKey()

  const query = useQuery(
    {
      ...getCacheHealthQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetCacheHealth500>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
