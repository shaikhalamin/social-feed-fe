/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetPostQueryResponse,
  GetPostPathParams,
  GetPost400,
  GetPost401,
  GetPost404,
  GetPost500,
} from '../types/GetPost.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { getPost } from '../clients/getPost.ts'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getPostQueryKey = ({
  id,
}: {
  id: GetPostPathParams['id'] | undefined
}) => [{ url: '/posts/:id', params: { id: id } }] as const

export type GetPostQueryKey = ReturnType<typeof getPostQueryKey>

export function getPostQueryOptions(
  { id }: { id: GetPostPathParams['id'] | undefined },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPostQueryKey({ id })
  return queryOptions<
    GetPostQueryResponse,
    ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>,
    GetPostQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getPost(
        { id: id! },
        { ...config, signal: config.signal ?? signal },
      )
    },
  })
}

/**
 * {@link /posts/:id}
 */
export function useGetPost<
  TData = GetPostQueryResponse,
  TQueryData = GetPostQueryResponse,
  TQueryKey extends QueryKey = GetPostQueryKey,
>(
  { id }: { id: GetPostPathParams['id'] | undefined },
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPostQueryResponse,
        ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getPostQueryKey({ id })

  const query = useQuery(
    {
      ...getPostQueryOptions({ id }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetPost400 | GetPost401 | GetPost404 | GetPost500>
  > & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
