/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetDbHealthQueryResponse,
  GetDbHealth500,
  GetDbHealth503,
} from '../types/GetDbHealth.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@/lib/kubb-clients/api-client'
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query'
import { getDbHealth } from '../clients/getDbHealth.ts'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getDbHealthSuspenseQueryKey = () =>
  [{ url: '/health/db' }] as const

export type GetDbHealthSuspenseQueryKey = ReturnType<
  typeof getDbHealthSuspenseQueryKey
>

export function getDbHealthSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getDbHealthSuspenseQueryKey()
  return queryOptions<
    GetDbHealthQueryResponse,
    ResponseErrorConfig<GetDbHealth500 | GetDbHealth503>,
    GetDbHealthQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getDbHealth({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * {@link /health/db}
 */
export function useGetDbHealthSuspense<
  TData = GetDbHealthQueryResponse,
  TQueryKey extends QueryKey = GetDbHealthSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetDbHealthQueryResponse,
        ResponseErrorConfig<GetDbHealth500 | GetDbHealth503>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getDbHealthSuspenseQueryKey()

  const query = useSuspenseQuery(
    {
      ...getDbHealthSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<GetDbHealth500 | GetDbHealth503>
  > & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
